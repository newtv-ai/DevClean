# Ollama API fixtures

Synthetic transcripts matching the documented Ollama loopback API response shapes for
`GET /api/version`, `GET /api/tags`, and `GET /api/ps`. They contain no user data and are
used only for strict parser tests.

