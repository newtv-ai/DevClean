# VS Code extension manifest fixtures

These fixtures model only the bounded `package.json` fields consumed by the
filesystem-only adapter. No `code` or `code-insiders` transcript exists because
the adapter is forbidden from launching either CLI.
